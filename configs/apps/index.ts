import { message as database } from "database";
import { message as ui } from "ui";
import { message as libs } from "libs";

console.log(`${database} from database`);
console.log(`${ui} from ui`);
console.log(`${libs} from libs`);
